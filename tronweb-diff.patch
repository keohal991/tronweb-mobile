diff --git a/src/lib/TransactionBuilder/TransactionBuilder.ts b/src/lib/TransactionBuilder/TransactionBuilder.ts
index c43a065..4d039ba 100644
--- a/src/lib/TransactionBuilder/TransactionBuilder.ts
+++ b/src/lib/TransactionBuilder/TransactionBuilder.ts
@@ -2459,8 +2459,17 @@ export class TransactionBuilder {
             if (typeof transaction.visible === 'boolean') {
                 res.transaction.transaction.visible = transaction.visible;
             }
+            const _transaction = res.transaction.transaction;
+            /* @ts-expect-error ignore */
+            if(transaction.payInfo){
+                /* @ts-expect-error ignore */
+                _transaction.payInfo = {
+                    /* @ts-expect-error ignore */
+                    ...transaction.payInfo
+                }
+            }
             return resultManager(
-                res.transaction.transaction as unknown as TransactionWrapper,
+                _transaction as unknown as TransactionWrapper,
                 {
                     ...transaction.raw_data.contract[0].parameter.value,
                     Permission_id: transaction.raw_data.contract[0].Permission_id,
diff --git a/src/lib/TransactionBuilder/helper.ts b/src/lib/TransactionBuilder/helper.ts
index 79f70cf..6b2cb70 100644
--- a/src/lib/TransactionBuilder/helper.ts
+++ b/src/lib/TransactionBuilder/helper.ts
@@ -41,6 +41,14 @@ export function resultManagerTriggerSmartContract(
     if (!(options._isConstant || options.estimateEnergy)) {
         const authResult = txCheckWithArgs(transaction.transaction, data, options);
         if (authResult) {
+            /* @ts-expect-error ignore */
+            if(data && data.function_selector){
+                /* @ts-expect-error ignore */
+                transaction.transaction.payInfo = {
+                    /* @ts-expect-error ignore */
+                    function_selector: data.function_selector
+                }
+            }
             return transaction;
         }
         throw new Error('Invalid transaction');
